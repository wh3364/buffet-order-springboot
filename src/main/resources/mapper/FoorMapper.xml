<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fch.buffetorder.mapper.FoodMapper">

    <!--查询所有食物-->
    <select id="queryAllFoods" resultType="Food">
        SELECT food_id,
               food_name,
               food_img,
               food_cate,
               have_detail,
               food_detail,
               cate_name,
               food_note,
               food_price
        FROM food
                 JOIN cate
                      ON food.food_cate = cate.cate_id
        WHERE food.is_enable = 1
    </select>


    <select id="queryFoodById" parameterType="Food" resultType="Food">
        SELECT food_id,
               food_name,
               have_detail,
               food_detail,
               food_price
        FROM food
        WHERE food.is_enable = 1 AND food_id = #{foodId}
    </select>


    <select id="queryFoodImgUrlById" parameterType="Food" resultType="Food">
        SELECT food_id,
               food_img
        FROM food
        WHERE food_id = #{foodId}
    </select>
</mapper>